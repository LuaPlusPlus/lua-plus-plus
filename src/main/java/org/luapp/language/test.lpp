class cat extends animal
    constructor(name)
        self.name = name
    end

    function isCat()
        return true
    end
end

print("hello world?")

local b = new cat("param")
local b = cat:new("param")