class ogl_suits.abilityMeta

    name {get set}

    constructor(name)
       self.name = name
    end

    function addHook(hookName, func)
        hook.Add(hookName, hookName .. "suits_ability", func)
    end

end